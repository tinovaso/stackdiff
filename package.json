{
  "name": "stackdiff",
  "version": "0.1.0",
  "description": "CLI tool to compare and visualize differences between two npm package dependency trees",
  "main": "dist/index.js",
  "bin": {
    "stackdiff": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "ts-node src/cli.ts",
    "test": "jest",
    "lint": "eslint src/**/*.ts"
  },
  "dependencies": {
    "commander": "^11.0.0",
    "chalk": "^5.3.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^20.0.0",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.1.0"
  },
  "keywords": ["npm", "dependencies", "diff", "cli"],
  "license": "MIT"
}
